using UnityEngine;

[DisallowMultipleComponent]
public class Holdable : MonoBehaviour
{
    // Marker component; ensure a non-kinematic Rigidbody is on the same GameObject.
    // You can extend this later (e.g., network authority rules, custom mass limits, sounds, etc.).
}